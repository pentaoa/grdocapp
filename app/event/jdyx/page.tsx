import type { Metadata } from 'next';
import Link from 'next/link';

export const metadata: Metadata = {
    title: '近大远小',
    description: '朋友们',
  };

export default async function JdyxPage() {
    return (
        <div>
            错误！(暂未更新)
        </div>
    )
}